# Aurora Mall API Notes

Aurora Mall still uses server-rendered Thymeleaf pages for most user-facing flows, but the admin and mall interactions rely on JSON endpoints behind the templates.

Document next:

- admin authentication endpoints
- carousel and homepage configuration endpoints
- goods CRUD endpoints
- cart and order workflow endpoints
- upload endpoint contract
